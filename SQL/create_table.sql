/*
CREATE TABLE Person_User(
                            name VARCHAR(30) NOT NULL,
                            email VARCHAR(20) NOT NULL,
                            password VARCHAR(15) NOT NULL,
                            description VARCHAR(40),
                            role VARCHAR(10),
                            id_person INT,
                            fcoins float
);

CREATE TABLE Artist(
                       description VARCHAR(40),
                       id_person INT
);

CREATE TABLE Buyer(
                      fcoins float,
                      id_person INT
);

CREATE TABLE Part(
                     title VARCHAR(20),
                     id_collection INT,
                     categori_part VARCHAR(15),
                     description VARCHAR(30),
                     fcoins float
);

CREATE TABLE Collection(
                           id_collection INT,
                           title VARCHAR(20),
                           picture VARCHAR,
                           description VARCHAR(30)
);
*/